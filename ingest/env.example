# .env.example — Ingestion (Sample)
# © 2025 Ryan Shaffer. All rights reserved.
# SAMPLE INGESTION ONLY — The full, production ETL/DDL codebase will be published soon.
#
# This file lists environment variables referenced by the sample ingestion scripts.
# Do NOT put real secrets here. Commit this template, not an actual .env with credentials.

#############################
# Required by the scripts
#############################

# Folder that contains your CarrierClaims CSV files.
# Used by example_carrier_claims_parallel_load_call.py when --folder is omitted.
DATA_DIR=/absolute/path/to/CarrierClaims

#############################
# Optional tuning
#############################

# Degree of parallelism for util.import_table. CLI --threads overrides this.
THREADS=8

#############################
# Operator convenience (optional)
#############################
# NOTE: These are NOT read by the Python scripts. They are here so you can
# build your mysqlsh command easily in your shell/CI. Use the -p flag to prompt
# for a password at runtime; do not store passwords in env files.

MYSQLSH_USER=root
MYSQLSH_HOST=localhost
MYSQLSH_PORT=3306

# Example URI format for reference (the scripts do not read this variable):
# MYSQLSH_URI=${MYSQLSH_USER}@${MYSQLSH_HOST}:${MYSQLSH_PORT}

#############################
# Notes
#############################
# - The scripts run under: mysqlsh --py (Classic Protocol) and expect session/util
#   to be available from __main__ (provided by mysqlsh).
# - Schema and table are required CLI flags (not env):
#     --schema synpuf --table carrier_claims
# - CSV dialect defaults (in code): fieldsTerminatedBy=",", linesTerminatedBy="\r\n", characterSet="utf8mb4"
